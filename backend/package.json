{
  "name": "ai-food-passport-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Mock backend proxy skeleton for AI Food Passport.",
  "type": "commonjs",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test:contract": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
