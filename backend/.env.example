PORT=8787

# This file is a placeholder template only.
# Do not commit a real .env file.
# Do not place provider keys in Flutter code.

# Safe default. Leave empty or use mock_ocr unless a future phase enables real OCR.
# Supported values:
# - mock_ocr
# - qwen_ocr_skeleton
# - google_vision_skeleton
# - openai_vision_skeleton
OCR_PROVIDER=mock_ocr

# Safe default. Leave empty or use mock_ai unless a future phase enables real analysis.
# Supported values:
# - mock_ai
# - qwen_analysis_skeleton
# - deepseek_analysis_skeleton
# - openai_analysis_skeleton
ANALYSIS_PROVIDER=mock_ai

# Future provider keys belong on the backend only.
# Leave these empty unless a future real provider phase explicitly wires them.
QWEN_API_KEY=
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
GOOGLE_VISION_API_KEY=
EXCHANGE_RATE_API_KEY=

# Future provider safety limits. These are placeholders until real providers are implemented.
PROVIDER_TIMEOUT_MS=15000
PROVIDER_MAX_RETRIES=0
PROVIDER_MONTHLY_BUDGET_USD=
PROVIDER_DAILY_REQUEST_LIMIT=
